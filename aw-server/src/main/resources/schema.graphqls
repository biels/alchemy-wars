type Query {
    lobbyServer(id: Int!): LobbyServer
    allLobbyServers(): [LobbyServer]
}

type Mutation {
    registerServer(port: Int!): LobbyServer
}

type LobbyServer {
    id: Int!,
    port: Int!
}